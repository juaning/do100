import React from 'react';
import CameraRecord from './CameraRecord';
import Routes from '../../config/routes';

const CameraRecordContainer = props => {
  return (
    <CameraRecord />
  );
}

export default CameraRecordContainer;
